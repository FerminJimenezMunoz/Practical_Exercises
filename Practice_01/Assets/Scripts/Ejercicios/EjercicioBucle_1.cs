using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EjercicioBucle_1 : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        for (int i = 0; i < 11; i++)
        {
            int operacion = 1 * i;
            Debug.Log("1 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 2 * i;
            Debug.Log("2 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 3 * i;
            Debug.Log("3 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 4 * i;
            Debug.Log("4 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 5 * i;
            Debug.Log("5 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 6 * i;
            Debug.Log("6 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 7 * i;
            Debug.Log("7 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 8 * i;
            Debug.Log("8 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 9 * i;
            Debug.Log("9 x " + i + " = " + operacion);
        }
        for (int i = 0; i < 11; i++)
        {
            int operacion = 10 * i;
            Debug.Log("10 x " + i + " = " + operacion);
        }
    }

    // Update is called once per frame
    private void Update()
    {
    }
}